import { SafeRemoveDirective } from './safe-remove.directive';

describe('SafeRemoveDirective', () => {
  it('should create an instance', () => {
    const directive = new SafeRemoveDirective();
    expect(directive).toBeTruthy();
  });
});
