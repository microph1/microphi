import { AnimateTextDirective } from './animate-text.directive';

describe('AnimateTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateTextDirective();
    expect(directive).toBeTruthy();
  });
});
